import * as assert from 'assert'

describe('Dummy class', () => {

  describe('dummy test', () => {
    it('should pass!', (done) => {
      assert.equal(true, true, 'Your tests are written badly!')
      done()
    })
  })

})
