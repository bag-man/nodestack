export default (app: object) => {
  // app.get('/api/reverse', (req: Object, res: Object) => {
  //   let reverse = req.query.string.split('').reverse().join('')
  //   res.json({ reverse })
  // })

  return app
}
